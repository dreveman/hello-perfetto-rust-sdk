[package]
name = "hello-perfetto-rust-sdk"
version = "0.1.1"
edition = "2024"

[features]
default = []
intrinsics = ["perfetto-sdk/intrinsics"]

[dependencies]
perfetto-sdk = "0.2"
perfetto-sdk-derive = "0.2"

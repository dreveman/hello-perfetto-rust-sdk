[package]
name = "hello-perfetto-rust-sdk"
version = "0.1.2"
edition = "2024"

[features]
default = []
intrinsics = ["perfetto-sdk/intrinsics"]

[dependencies]
perfetto-sdk = "1"
perfetto-sdk-derive = "1"

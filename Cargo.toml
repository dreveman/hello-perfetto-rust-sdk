[package]
name = "hello-perfetto-rust-sdk"
version = "0.1.0"
edition = "2024"

[features]
default = []
intrinsics = ["perfetto-sdk/intrinsics"]

[dependencies]
perfetto-sdk = "0.1.2"
perfetto-sdk-derive = "0.1"
